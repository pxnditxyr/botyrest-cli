import { green } from '../utils'

export const printMainMenuInterface = () => {
  console.log( `${ green( '================================' ) }` )
  console.log( `${ green( '            Select an option' )     }` )
  console.log( `${ green( '================================' ) }` )
}
