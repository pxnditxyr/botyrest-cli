import { green } from '../utils'

export const selectOptionMessage = () => (
`Select an option and press ${ green( 'ENTER' ) }:`
)

export const pauseMessage = () => (
`Press ${ green( 'ENTER' ) } to continue\n`
)
  
